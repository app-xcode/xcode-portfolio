# x.code — Developer Portfolio

Responsive static portfolio inspired by the supplied reference design.

## Stack
HTML5 · CSS3 · Vanilla JavaScript · Font Awesome · Google Fonts

## Run locally
Open `index.html` or use `python3 -m http.server 5500`.

## GitHub Pages
Settings → Pages → Deploy from branch → `main` → `/ (root)`.
